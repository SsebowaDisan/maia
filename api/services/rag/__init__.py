"""Maia RAG Pipeline — clean, phase-by-phase document intelligence.

14 phases: upload → classify → extract → normalize → chunk → embed → index →
citation_prep → retrieve → rerank → coverage → answer → citations → deliver.

Every phase preserves evidence geometry so citations can highlight exact source text.
"""
